export const API_CONFIG = {
  BASE_URL: 'http://160.250.132.226',
  API_PATH: '/api', // This might need to be changed based on actual server setup
  TIMEOUT: 10000,
  RETRY_ATTEMPTS: 3,
  RETRY_DELAY: 1000,
};
